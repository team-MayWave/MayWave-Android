<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MayWave" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.MayWave.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/maywave_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.MayWave</item>
    </style>
</resources>
